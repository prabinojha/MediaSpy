<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('includes/head', { title: 'Update Reviews' }) %>
</head>
<body>
    <%- include('includes/header') %>
    <main class="container">
        <h1>Update Review</h1>
        <form action="/reviews/update/<%= review.id %>" method="POST">     
            <input type="text" id="name" name="name" value="<%= review.name %>" required />
            <select id="type" name="type" required>
                <option value="movie" <%= review.type === 'movie' ? 'selected' : '' %>>Movie</option>
                <option value="video_game" <%= review.type === 'video_game' ? 'selected' : '' %>>Video Game</option>
            </select>
            <select id="age" name="age" required>
                <option value="general" <%= review.age === 'general' ? 'selected' : '' %>>General (G)</option>
                <option value="parentalGuidance" <%= review.age === 'parentalGuidance' ? 'selected' : '' %>>Parental Guidance (PG)</option>
                <option value="mature" <%= review.age === 'mature' ? 'selected' : '' %>>Mature (M)</option>
                <option value="restrictedR" <%= review.age === 'restrictedR' ? 'selected' : '' %>>Restricted (R 18+)</option>
                <option value="restrictedX" <%= review.age === 'restrictedX' ? 'selected' : '' %>>Restricted (X 18+)</option>
            </select>
            <textarea id="content" name="content" required><%= review.content %></textarea>
            <input type="number" id="rating" name="rating" min="1" max="5" value="<%= review.rating %>" required />
            <input type="text" id="company_name" name="company_name" value="<%= review.company_name %>" required />
            <input type="text" id="theme" name="theme" value="<%= review.theme %>" required />
            <button type="submit">Update</button>
        </form>
    </main>
</body>
</html>