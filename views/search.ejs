<!DOCTYPE html>
<html lang="en">
<%- include('includes/head', { title: 'Search Results' }) %>

    <body>
        <%- include('includes/header') %>

            <div class="search-results-container">
                <h2>Search Results for "<%= query %>"</h2>
                <hr>

                <div class="results-section">
                    <h2>Movies</h2>
                    <% if (movieResults.length> 0) { %>
                        <ul class="results-list">
                            <% movieResults.forEach(function(movie) { %>
                                <li class="result-item">
                                    <a href="/view-content/<%= movie.id %>" class="result-link">
                                        <img src="<%= movie.image_path || 'https://via.placeholder.com/100' %>"
                                            alt="<%= movie.name %>" class="result-image">
                                        <div class="result-info">
                                            <div class="searchTitle">
                                                <%= movie.name %>
                                            </div>
                                            <p
                                                class="searchRating <%= movie.rating > 4 ? 'high-rating' : movie.rating >= 3 ? 'medium-rating' : 'low-rating' %>">
                                                Viewers Rated <strong>
                                                    <%= movie.rating %>/5
                                                </strong>
                                            </p>
                                            <p><strong>Theme:</strong>
                                                <%= movie.theme %>
                                            </p>
                                            <p><strong>Company:</strong>
                                                <%= movie.company_name %>
                                            </p>
                                        </div>
                                    </a>
                                </li>
                                <% }) %>
                        </ul>
                        <% } else { %>
                            <p>No movies found.</p>
                            <% } %>
                </div>

                <div class="results-section">
                    <h2>Video Games</h2>
                    <% if (videoGameResults.length> 0) { %>
                        <ul class="results-list">
                            <% videoGameResults.forEach(function(game) { %>
                                <li class="result-item">
                                    <a href="/view-content/<%= game.id %>" class="result-link">
                                        <img src="<%= game.image_path || 'https://via.placeholder.com/100' %>"
                                            alt="<%= game.name %>" class="result-image">
                                        <div class="result-info">
                                            <div class="searchTitle">
                                                <%= game.name %>
                                            </div>
                                            <p
                                                class="searchRating <%= game.rating > 4 ? 'high-rating' : game.rating >= 3 ? 'medium-rating' : 'low-rating' %>">
                                                Viewers Rated <strong>
                                                    <%= game.rating %>/5
                                                </strong>
                                            </p>
                                            <p><strong>Theme:</strong>
                                                <%= game.theme %>
                                            </p>
                                            <p><strong>Company:</strong>
                                                <%= game.company_name %>
                                            </p>
                                        </div>
                                    </a>
                                </li>
                                <% }) %>
                        </ul>
                        <% } else { %>
                            <p>No video games found.</p>
                            <% } %>
                </div>

            </div>


    </body>

</html>